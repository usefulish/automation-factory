# BEGIN swamp managed section - DO NOT EDIT

# Runtime data (not needed in version control)
.swamp/

# Local extension sources (developer-specific, not shared)
.swamp-sources.yaml

# Workbuddy instructions (managed by swamp)
.workbuddy-ai/swamp-instructions.md
# END swamp managed section

# Deno tooling (extension deps are bundled by swamp, not lockfile-tracked)
node_modules/
